Recording Studio AI installation complete.

Next steps:

1. Review `config/initializers/recording_studio_ai.rb`.
2. Optionally prepare OpenAI and/or Gemini credentials for a future provider adapter.
3. Confirm Recording Studio is configured in the host application.
4. Review the generated engine mount path in `config/routes.rb`.

No AI provider is selected and no provider SDK is required in Phase 1. This
phase does not install migrations or expose generation endpoints.
